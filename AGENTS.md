# AGENTS.md

## 项目定位

这是一个面向教学场景的像素风 Web 端 RPG Agent 世界。产品目标不是做纯娱乐游戏，而是把学生学习和使用 Agent、构建系统、协作互测这件事游戏化。

## 高价值方法论

### 1. 双层架构法

当一个产品同时追求“好玩”与“可教学”时，不要把世界表现和业务真相揉在一起。

- 下层是真实教学系统：账号、课程关卡、Agent 提交、评分、老师工作台、工会、任务、交易介质、授权。
- 上层是像素 RPG 世界：主城区、家园、工会大厅、聊天室入口、小人状态、建筑交互。
- 所有影响规则、权限、评分、积分和关卡解锁的逻辑，都以下层为准。
- 上层只负责把这些能力可视化和游戏化，不能成为唯一业务真相来源。

适用场景：

- 教学平台游戏化
- 运营系统加世界化表达
- 需要长期扩展 UI 但不希望重写核心业务的项目

### 2. 先教学闭环，后世界增强

遇到“沉浸式产品”开发时，优先级不要被地图和动画绑架。

推荐顺序：

1. 能教：课程关卡、任务、提交标准成立
2. 能提：Agent 提交协议和老师工作台成立
3. 能评：Agent 初评和老师裁定成立
4. 能协作：工会、授权进入、互测、贡献记录成立
5. 能沉浸：主城区、家园、小人和像素表现增强

这样做能避免“外观很完整，但老师不能用、学生无法提交、课程跑不起来”的假繁荣。

### 3. 账号角色与世界身份分离

如果系统本质是教学平台，真实账号应尽量少，世界身份应做成关系状态。

- 真实账号只保留：老师、学生
- 世界身份做成关系态：会长、成员、访客、互测者、协作者
- 会长不是平台账号类型，而是某个学生在某个工会中的关系身份

这样有助于降低权限复杂度，并让产品更贴近真实教学组织。

### 4. Agent 接入协议化，而非厂商绑定

当系统要接入 Codex、Claude Code 或未来的新 Agent 架构时，不要把平台写死在某一家 SDK 上。

- 平台内部统一抽象：`AgentSession`、`AgentSubmission`、`AgentCapability`
- 外部 Agent 通过适配器接入
- 采用开放式协议边界，优先兼容 MCP、A2A 这类分层思路

这样后续替换或新增 Agent 提供方时，只需要增加适配器，而不是重写主系统。

### 5. 实时系统只同步必要状态

40+ 在线的教学世界不需要一开始做成重型 MMO。

- 地图层实时同步：在线状态、位置、动作、房间、忙碌状态
- 业务层异步处理：提交、评分、奖励、任务结算
- 高价值数据走后端真相，世界表现走轻量实时广播

这能在保证课堂稳定性的同时保留“在线世界”的沉浸感。

### 6. 验证命令必须由交付物直接支撑

写实施计划时，验收命令不能依赖“未出现在交付物列表中的隐含文件”。

- 如果验收命令是 `tsc --showConfig`，仓库根目录就必须真的存在可被默认发现的 `tsconfig.json`，而不能只有 `tsconfig.base.json`
- 对 TypeScript 这类工具链，还要确认配置不是“空工程”，必要时补一个最小输入文件，避免命令因 `No inputs were found` 失败
- 如果某个验证命令需要额外入口文件、配置文件或目录结构，就把这些文件显式写进任务清单
- `.gitignore`、提交步骤和验收产物必须互相一致，例如需要提交 `pnpm-lock.yaml` 时就不能同时把它忽略
- 先写失败命令，再补最小文件集让命令转绿，避免“代码看起来齐了，但验收命令跑不通”

这能减少计划和实际落地之间的偏差，让 TDD 的红绿循环建立在真实可执行的命令之上。

### 7. 共享契约先行法

当多个应用会共同消费同一份业务数据时，先把共享契约做成独立包，再让上下游依赖它，而不是各自手写类型。

- 先定义共享 Zod schema，再从 schema 反推出 TypeScript 类型，避免“类型能过、运行时却收脏数据”
- 关键输入边界先写契约测试，至少覆盖一个完整合法 payload，确保 schema 真能支撑主流程
- monorepo 里的共享契约包要独立 `build` 和 `test`，让 API 与 Web 在接入前就能发现契约漂移
- 计划里的验证命令如果依赖新 workspace 包，先确认锁文件和依赖安装策略可运行，避免被 `frozen-lockfile` 之类环境差异卡住
- 提交、评审、授权这类教学核心对象优先进入共享契约层，像素世界表现层只消费结果，不自行定义业务真相

这样能把教学系统的真实规则收拢到统一边界，降低前后端、实时层和评审流之间的契约分叉风险。

### 8. 数据库形状先锁定，seed 再表达世界快照

当教学世界开始进入数据库建模阶段时，先用测试锁定“必须存在哪些核心模型与关系”，再去扩 schema 和 seed；不要先堆模型，最后才靠肉眼检查。

- 先写数据库形状测试，至少断言课程世界、家园、工会、提交、评审、贡献记录这些核心模型真实出现在 schema 中
- schema 不只列字段，还要尽早补齐关键关系和枚举，让 `prisma format`、`prisma generate` 这种机器验证命令能尽快暴露建模错误
- seed 不要停留在“占位日志”，应当表达一个可理解的教学起始场景，例如 1 个老师、3 个学生、1 个工会、若干 Day 任务
- 把 seed 视为“世界快照样本”，它首先服务于沟通和验收，其次才是本地初始化；读 seed 就应能看懂业务世界的最小闭环
- TDD 顺序保持明确：先让 schema 测试失败，再补最小模型和种子数据转绿，最后跑 Prisma 生成命令确认模型可被工具链接受

这样能避免数据库层出现“字段很多但世界关系不清”“seed 能跑但看不出教学闭环”的问题，也让后续 API、评审流和前端世界壳层有稳定的数据地基。

### 9. 教学闭环接口先做流程测试，再拆控制器

当一个功能横跨工会、房间授权、提交、评审几个模块时，不要先按目录把控制器一个个写出来；先用一条端到端的流程测试把闭环钉住，再补最小接口实现。

- 先写 `submission-flow` 这类集成测试，一次覆盖 `guilds`、`rooms/access-grants`、`submissions`、`reviews/decide` 四段关键路径
- 红灯阶段要确认失败原因真的是“接口不存在或未注册”，而不是测试数据写错、依赖没起、路由拼错
- 绿灯阶段只补最小控制器和 `AppModule` 注册，让测试刚好通过，避免一开始就引入 service、数据库、队列等未被当前任务验证的复杂度
- 提交接口要同时返回“提交对象”和“建议评审结果”，这样老师工作台与后续队列都能围绕同一份后端真相扩展
- 手工探测保留给高价值节点，例如 `POST /reviews/decide`，用来确认测试之外的真实 HTTP 路由和序列化输出也符合预期

这样能把“能提交、能初评、能老师裁定、能协作授权”的教学闭环尽早跑通，减少按模块分头实现后才发现接口拼不起来的返工。

### 10. Phaser 只做客户端世界壳，SSR 与测试环境不直连引擎

当教学平台把 Phaser 接进 Next.js App Router 时，不要让游戏引擎直接参与服务端渲染或 jsdom 测试；世界壳先稳定，画布再在浏览器端延迟启动。

- 页面层先渲染稳定的 React/HTML 壳：标题、说明文案、挂载容器，这些都应该在不启动 Phaser 的情况下可测试、可构建、可输出 HTML
- Phaser 场景文件避免在模块顶层强依赖浏览器能力，优先在 `useEffect` 或异步工厂里动态加载，防止 `window`、`canvas`、WebGL 在 SSR 阶段报错
- `WorldShell` 只负责“挂载点 + 生命周期”，真正的业务数据仍从 API client 获取，保持世界表现层是薄壳，不把教学真相塞进场景对象
- 测试环境显式跳过 Phaser 启动，只验证页面标题、世界挂载点和 API client 行为，避免被 `HTMLCanvasElement.getContext` 之类 jsdom 限制干扰
- 构建验收不能只看 Vitest 通过，还要补 `next build` 和一次真实 HTTP 探测，因为 Vite 能跑通的动态导入/类型写法，不一定能通过 Next 的类型检查和产物构建

这样能把像素世界接入保持在“可视化增强层”，既不破坏 Next.js 的 SSR/构建稳定性，也能让教学主页和世界壳在早期阶段先可靠落地。

### 11. 多应用开发端口要显式钉死，不依赖默认值

当 monorepo 里同时跑 Web、API、Playwright、Socket 或其他本地服务时，不要把端口留给工具默认行为或临时环境变量碰运气。

- 每个应用的开发端口都应写死在各自脚本中，例如 Web 固定 `3000`、API 固定 `3001`，避免团队成员机器上因默认端口漂移产生“我这里能跑、你那里冲突”的问题
- 实施计划、README、测试命令和本地脚本里的端口要保持同一套口径，不能文档写 `3000`、脚本却实际随机起在别的端口
- 只要页面测试、手工验证或 E2E 用到了本地 URL，就应优先依赖显式端口，而不是依赖 Next/Nest 的默认值
- 端口分配应体现职责边界：用户界面、业务 API、实时服务、测试代理尽量分层，不复用、不混用
- 如果未来需要通过环境变量覆盖端口，也应让“默认值本身”是稳定且被提交进仓库的，而不是完全空置

这样能减少本地联调、自动化测试和文档验收之间的环境偏差，尤其适合前后端并行开发的教学平台 monorepo。

### 12. 提交受理要同步确认，评审处理要异步排队

当学生一次“今日提交”同时触发老师工作台、实时世界状态和后端评审时，不要让前端等异步评审跑完，也不要把队列受理结果藏在服务内部。

- `POST /submissions` 这类教学主入口应同步返回三层信息：`submission`、当前可展示的 `review` 快照、以及可追踪的 `queue` 回执
- `queue` 至少要包含稳定的 `jobId` 和明确的 `status`，这样老师工作台、轮询接口或后续 WebSocket 更新才能围绕同一任务继续追踪
- 实时网关优先广播轻量世界状态，例如 `presence:update`，不要把评分、裁定、积分结算这类业务真相直接塞进实时层
- Nest 接入 gateway 时要把 provider 注册和 WebSocket driver 依赖一起补齐，避免“代码已写但测试和运行环境因缺 driver 直接失败”
- TDD 顺序保持明确：先让“提交返回队列状态”和“网关广播指定事件”失败，再补最小 queue service、gateway 和控制器注入

这样能把“学生提交已被系统接收”与“后台评审正在异步处理”清晰分层，既保证课堂交互反馈及时，也为后续扩展真实队列、重试和老师工作台追踪留出稳定接口。

### 13. 先用跨页烟雾测试守住入口，再扩展深层流程

当教学平台已经有主页、老师工作台、工会大厅这类关键入口时，不要一上来就写大量重型 E2E；先用一条最小跨页烟雾测试确认“应用能启动、页面能打开、核心标题可见”。

- 烟雾测试优先覆盖真正代表教学闭环入口的页面，例如主城区和老师工作台，而不是堆很多样式细节断言
- TDD 顺序保持明确：先写会失败的 Playwright 用例，确认失败原因真的是缺少依赖、配置或页面入口，再补最小支撑文件让它转绿
- 浏览器测试配置要尽量自包含，优先由 Playwright 自己拉起本地 Web 服务，减少“文档要求先手动开服务，否则测试必挂”的脆弱依赖
- README runbook 要和仓库真实脚本一一对应，尤其是浏览器安装命令、开发端口、测试命令，避免文档能读但无法照着复现
- 生成型产物如 `playwright-report`、`test-results` 应及时进入 `.gitignore`，防止一次验收后把工作区弄脏，干扰真正的交付 diff

这样能先把“这个教学世界至少能被打开、被看到、被老师进入”的最小可信度钉住，再在其上逐步扩展提交、评审、授权和实时协作的深层端到端验证。

### 14. E2E 端口独立于开发端口与 API 端口

当 monorepo 同时存在本地开发 Web、业务 API 和 Playwright 自拉起的测试 Web 服务时，不要让 E2E 复用开发端口；否则很容易出现 `3000` 被占用、测试误连现有服务或本地联调相互干扰的问题。

- 开发态用户入口和 E2E 测试入口应显式分配不同端口，例如开发 Web 固定 `3000`，Playwright 固定 `3100`，API 固定 `3001`
- `playwright.config.ts` 里的 `use.baseURL`、`webServer.url` 与启动命令要引用同一套 E2E 端口，避免配置改了一半
- smoke 用例优先使用相对路径如 `/`、`/teacher`，把端口决策收口到 Playwright 配置层，不在测试文件里写死 `3000`
- README、脚本名和测试命令要同步说明 E2E 会自动拉起自己的 Web 服务，减少“本地先手动起哪个端口”的歧义
- TDD 顺序保持明确：先写一个会因端口仍指向 `3000` 而失败的配置测试，再补最小脚本和配置让它转绿

这样能把本地开发、API 联调和浏览器自动化测试彼此解耦，降低端口冲突与误连环境导致的假失败。

### 15. 先锁定可本地自举的 Prisma 底座，再去掉接口硬编码

当某个 API 端点要从“控制器硬编码返回”切到“真实 Prisma 读写”时，不要直接把查询塞进控制器，然后才发现测试环境里根本没有可用数据库；先把 Prisma 的本地可执行底座锁定住，再做端点改造。

- 先让仓库在本地与 CI 中都能自举出可运行数据库，再写依赖数据库结果的集成测试；如果外部 Postgres 在当前阶段并不稳定，可优先使用仓库内可重复创建的 SQLite Prisma datasource 作为 TDD 底座
- 集成测试应显式完成三步：设置独立 `DATABASE_URL`、执行 `prisma db push`、再调用共享 `seedDatabase(prisma)` 写入教学世界样本，而不是依赖开发者手动提前初始化
- `/world`、`/guilds` 这类去硬编码改造，测试不只断言 `POST` 返回体；还要追加一次后续 `GET`，确认写入结果真的可被 Prisma 再读出来
- seed 不应只是演示日志，最好导出成可复用的 `seedDatabase(prisma)`，这样 CLI、本地初始化和集成测试都围绕同一份世界快照运行
- 如果某次失败发生在“数据库没起来、schema 没推送、client 没生成”，先修测试底座，再继续业务红绿循环，避免把环境噪音误判成业务回归

这样能让“接口是否真的脱离硬编码”由可重复执行的 Prisma 读写测试来证明，而不是靠肉眼看控制器代码。

### 16. 结构化提交字段先整体持久化，接口契约再做回映射

当教学平台的提交接口除了核心外键和摘要，还带有 `artifacts`、`agentEvaluationHints`、`timestamp` 这类结构化字段时，不要先把它们留在控制器响应里“假装存在”，也不要急着为每个长尾字段拆很多表；先让数据库能完整接住这次提交，再由接口层把存储形态回映射成对外契约。

- 集成测试先断言两层真相：`POST /submissions` 的 HTTP 响应完整返回这些字段，以及后续 Prisma 查询也能把同一份字段读回来
- 对当前阶段仍主要作为提交快照的结构化字段，可优先采用 Prisma `Json` 字段整体落库，等真正出现独立查询、筛选、统计需求时再拆表
- 时间字段不要只信任 `createdAt`，应显式保存业务提交时间如 `submittedAt`，再在响应层回映射成契约里的 `timestamp`
- `POST /reviews/decide` 不只验证同步返回体，还要追加一次 Prisma 读取，确认老师裁定确实更新到了已有 `ReviewResult` 上
- 控制器保持薄层：内部可以用 `submittedAt`、JSON 字段和枚举；对外仍返回原契约字段名，避免前端因为存储层重构被迫联动修改

这样能在最小建模成本下先把“提交快照完整入库、评审结果真实持久化、外部契约保持稳定”三件事同时做到。

### 17. 聊天概览先做只读聚合，再补消息流与授权细化

当教学世界开始接聊天室页时，不要一上来就把实时消息、权限矩阵、已读状态和房间成员生命周期全部做满；页面在早期阶段最需要的是一份可信的真实上下文概览。

- 后端先提供 `GET /chat?studentId=...` 这类只读 `chat overview`，直接聚合学生、Agent 会话、最近提交与协作贡献，让聊天室页先摆脱硬编码
- 即使当前页面只服务默认学生，也优先把 `studentId` 放进查询参数，先锁定 read model 的消费边界，避免后续从页面常量回退到接口时重改契约
- 协作相关信息在早期可先基于 `ContributionLog`、工会成员或最近互测记录做最小推导，不必等待完整授权模型齐备后才让页面接真实数据
- 前端页面层负责请求 API 和把状态映射成展示文案，展示组件继续保持薄层，只消费 read model 结果，不自行拼业务真相
- TDD 顺序保持明确：先让 `GET /chat` 集成测试失败，再接通 `api-client`、页面测试与组件测试，确保“真实数据替换硬编码”由测试闭环证明

这样能把聊天室优先落在“教学上下文入口”而不是“重型 IM 系统”，既尽早转向真实业务数据，也为后续扩展消息流、授权和实时广播保留稳定接口。

### 17. 页面去硬编码时先贴真实接口，不为旧 UI 伪造教学数据

当 Web 端要把主页、工会大厅、老师工作台这类入口页从硬编码数据切到真实 API 时，如果后端暂时没有某个页面专属聚合接口，不要继续保留旧页面形态并用假数据顶着；应优先贴近现有真实接口，必要时同步收口组件展示方式。

- 页面层优先直接消费已经成立的教学真相接口，如 `/world`、`/guilds`、`/quests`，而不是再造一层前端假 DTO
- 主城区可展示 `currentDay`、在线家园数这类真实概览，像素世界壳仍保持独立挂载，不把业务真相塞进 Phaser
- 工会大厅如果当前只有工会列表接口，就先展示工会名、成员数、协作积分等真实字段，不要伪造 mission、成员状态和会长文案
- 老师工作台如果暂时没有评审队列读取接口，就先围绕真实 Day/Quest 状态展示“当前 Day、已完成、进行中、未解锁”等教学概览，等后端真接口到位后再扩展审核队列
- 组件 props 应随真实契约重构，让 UI 适配后端真相，而不是反过来逼迫接口为旧静态页面背锅

这样能让页面尽快摆脱硬编码，同时避免“看起来像真实工作台，实际上数据全是前端杜撰”的教学假闭环。

### 18. 老师工作台列表先做读模型，聚合统计与明细一起用测试钉住

当老师工作台需要展示“待审核数量 + 审核列表明细”时，不要让前端分别拼 `/submissions`、`/reviews`、`/guilds`、`/quests` 多条接口，也不要先塞一个只返回裸 `ReviewResult[]` 的半成品接口；应先在后端做最小只读聚合接口，并用集成测试同时锁住 `summary` 和 `items`。

- 老师视角的列表接口优先直接返回可渲染读模型，例如 `summary.pendingCount`、`summary.reviewedToday`、`summary.flaggedCount` 与 `items[]`
- `items[]` 至少聚合提交、评审、学生、Day、工会这些老师做判断时必须同屏看到的字段，避免前端二次串联多条请求拼业务真相
- 聚合统计要基于后端可解释真相计算，例如是否已有 `reviewerId`、是否被老师改判、是否命中 `adjust/reject`，不要依赖前端猜测状态
- `submission-flow` 这类集成测试应先造出至少两条不同时间的提交，再追加一次老师裁定，用真实持久化数据断言“最新在前”的列表顺序和统计数值
- 共享测试数据库的集成套件一旦开始覆盖读模型，就要在 `beforeEach` 里重置 seed，避免前一个用例创建的工会、提交或评审污染后一个列表断言

这样能让老师工作台尽快拿到稳定、可解释、可扩展的真实数据入口，也能减少后续 Web 页面接入时的接口拼装成本。

### 19. 老师工作台接真实双接口时，页面先并发取数，再在展示层做轻映射

当老师工作台既要展示 Day 关卡进度，又要展示审核队列时，不要继续拿 `/quests` 假装审核列表，也不要为了省事把后端返回结构直接泄漏进组件；应先让页面层并发请求真实接口，再在页面内做最小展示映射，把 `DayPanel` 与 `ReviewQueue` 都绑到真实数据上。

- TDD 先从 Web 侧开始，至少写红三个点：`api-client` 真请求 `/reviews`，`TeacherPage` 同时依赖 `/quests` 与 `/reviews`，`ReviewQueue` 渲染真实审核摘要与审核项
- 页面层优先用 `Promise.all` 并发请求老师工作台所需的多条真实接口，避免串行等待把老师入口做慢
- `DayPanel` 继续围绕 quest 真相渲染“当前 Day/已完成/未解锁”，`ReviewQueue` 则单独消费 review 真相，不再让一个接口冒充两种业务含义
- 展示层允许做轻量映射，例如把 `adjust` 映射为“需要调整”、把 ISO 时间映射成页面标签；但业务统计和审核状态判断仍以后端返回为准
- 验收不只跑组件测试，还要至少补一次 `web build` 与视情况的 `api test/build`，确认跨应用契约、SSR 与类型检查一起成立

这样能把“老师工作台真实可用”拆成稳定的数据获取层和轻量的展示映射层，避免页面继续依赖硬编码审核队列或把 Day 列表误当成审核业务真相。

### 20. Web 接真数据后，先把“服务端安全降级”钉住，再谈静态化

当 Next.js App Router 页面开始直接在服务端请求本地 API 时，不要默认假设 API 在 `build`、E2E 或独立预览环境里总是可达；应先把“API 不可达时页面仍能安全渲染”的降级路径做成明确能力，再决定哪些页面适合静态化。

- 先在 Web 侧写红灯：至少覆盖一条 `api-client` 取数失败回落空数据、一个页面在降级时仍能渲染标题与空态提示、以及一条不启动 API 的 E2E 烟雾测试
- 真实请求函数与安全包装函数分层：严格请求函数继续代表“必须拿到后端真相”，安全包装函数只在页面壳层消费，并显式返回 `degraded` 之类状态给 UI
- 页面在 API 可达时必须继续优先使用真实数据；只有在连接失败、超时或非 2xx 响应时，才回退到最小可展示空态，避免把“静默假数据”误当业务真相
- 对依赖实时 API 或本地开发 API 的页面，优先显式标注 `dynamic = "force-dynamic"`，不要让构建期预渲染偷偷把外部依赖变成 build 阶段硬前置
- 空态不应只是空数组；至少要给出可见提示文案，让老师和学生知道当前是“安全降级展示”，不是教学世界真的没有任务、工会或审核项

这样能同时守住三件事：API 在线时页面继续吃真实数据，API 离线时 `build`/E2E/预览环境不崩，以及团队能明确区分“后端真相为空”和“前端因环境缺依赖进入安全空态”。

### 21. 服务端页面守读，客户端叶子组件守写

当 Next.js App Router 页面既要保留服务端真实取数、又要触发聊天提交或老师裁定这类写动作时，不要试图把函数从 Server Component 直接下传到客户端组件；应让页面继续负责读取真相，让最靠近按钮的客户端叶子组件自己调用 API client。

- 页面层优先保留服务端并发取数、空态降级和展示映射，避免为了一个按钮把整页改成客户端组件
- 写动作收口到 `ChatRoom`、`ReviewQueue` 这类叶子组件内部，通过 `api-client` 直接调用 `POST /submissions`、`POST /reviews/decide`
- 页面只下传串行化后的真实标识与展示数据，例如 `studentId`、`dayId`、提交状态、评审项摘要，不下传函数
- TDD 顺序保持明确：先让叶子组件点击测试失败，再补 `api-client` 写接口测试，最后接上组件内部最小状态反馈
- 构建验收要补 `web build`，因为“服务端读 + 客户端写”的边界最容易在类型检查和 SSR 序列化阶段暴露问题

这样能同时保住服务端页面的真实数据读取能力、客户端写动作的交互体验，以及 App Router 下可构建可测试的组件边界。

### 22. 定向 API 验收要直连 Vitest 文件路径，不要把红绿循环交给模糊脚本过滤

当 API 仓库已经开始累积多条集成套件，而你只想对某个任务做 TDD 红绿循环时，不要默认依赖 `pnpm --filter api test -- auth-flow` 这类脚本转发；它可能仍会把无关测试一起跑进来，让基线噪音掩盖真正的失败原因。

- 红灯阶段优先直接使用 `pnpm --filter api exec vitest run test/auth-flow.spec.ts` 这类命令，显式钉住要执行的文件
- 如果一个任务会影响相邻读写流，就把受影响的相关文件一起列进命令，例如 `auth-flow`、`room-access`、`world-api`、`submission-flow`
- 先确认失败原因真的是当前能力缺失，例如 token 仍是假值、列表路由仍不存在，而不是被无关 suite 或历史基线失败打断
- 绿灯后再跑更宽一层的相关套件与 `api build`，把“局部红绿循环”和“任务级验收”分成两个清晰阶段
- README、计划文档和验收记录里的命令也要保持这套口径，避免协作者照着脚本跑却看到另一组失败

这样能让 TDD 的每一次失败和转绿都保持可解释，减少 monorepo 中无关测试基线对当前任务判断的干扰。

### 23. Redis 队列先懒连接，流程测试再用 provider override 隔离外部依赖

当教学平台把 BullMQ 真正接进提交流程时，不要让 `Queue` 在模块加载或测试启动阶段就强连本地 Redis；应先把连接创建收口到懒执行边界，再让集成测试显式覆盖或替换队列 provider。

- 队列服务优先暴露 `queueFactory` 或同等注入点，只在真正执行 `enqueue` 时才创建 BullMQ `Queue`
- 队列契约测试直接断言 `queue.add()` 的任务名、payload、`jobId` 与 `removeOnComplete/removeOnFail` 选项，避免被“只返回 queued 响应”的 stub 假绿
- 业务流集成测试如 `submission-flow` 应通过 `overrideProvider(ReviewQueueService)` 注入最小假实现，验证 HTTP 契约与数据库写入，而不是把测试稳定性绑死到本机 Redis
- 运行时文档要单独说明 `REDIS_URL` 与默认连接地址，让“本地没起 Redis”成为显式环境问题，而不是隐式构建故障
- 任务级验收保持两层：一层跑真实队列契约测试与 `api build`，一层跑隔离外部依赖后的提交流程测试，分别证明“队列接入成立”和“主流程未被队列拖垮”

这样能同时得到真实的 Redis/BullMQ 接入、稳定可解释的 TDD 红绿循环，以及不被外部基础设施噪音污染的教学主流程测试。

### 24. 根构建失败但单应用构建成功时，先怀疑 workspace 并行竞态

当 monorepo 里的 `pnpm build` 在仓库根失败，但 `pnpm --filter web build` 单独执行却稳定成功时，不要先假设是页面源码坏了；优先怀疑 workspace 递归并行构建触发了 Next.js 的构建竞态。

- 先分别验证两条命令：仓库根 `pnpm build` 与单应用 `pnpm --filter web build`；如果只有根命令失败，就说明问题更可能在工作区调度层，而不是 `app/page.tsx` 本身
- 遇到 `/_document` not found、`.next/types/... not found` 这类只在递归构建里出现、单应用构建却消失的报错时，先测试 `pnpm -r --workspace-concurrency=1 build` 是否转绿
- 如果顺序构建能稳定通过，优先把根脚本改成显式顺序构建，而不是急着改页面源码、删类型能力或在子应用里堆额外清理脚本
- `.next` 仍然是可再生产物，但“先清 `.next`”不是总能命中根因；若清理后又冒出 `/_document` 之类 Next 内部页面异常，应回到“并行调度是否触发竞态”这条主线排查
- 验收口径以仓库根命令为准：修复后至少同时验证 `pnpm test`、仓库根 `pnpm build` 和 `pnpm test:e2e`，确保不是“单个包绿、整体仍不稳定”

这样能把“源码错误”和“monorepo 构建调度问题”分开处理，减少把 Next.js 递归构建竞态误判成页面实现缺陷。

### 25. 房间授权先保留全量历史，聊天室只对当前生效项开放写操作

当聊天室页要从“协作者文案占位”切到“真实房间授权列表 + 撤销/创建授权”时，不要把撤销做成物理删除，也不要让前端自己猜哪些记录已经失效；应先把授权记录作为后端真相完整保留，再由展示层清楚区分 `approved` 与 `revoked`。

- `POST /rooms/access-grants/:id/revoke` 这类撤销接口优先做软停用，把 `status` 改成 `revoked`，保留原 `createdAt`、`expiresAt` 和 `granteeId`
- `GET /rooms/access-grants?roomId=...` 默认返回该房间全部授权历史，让聊天室页同时看到“生效中”与“已撤销”，避免后续追溯授权变更时丢真相
- 列表接口应直接补齐最小展示读模型，例如 `granteeName`，不要把“显示名查找”留给前端再串其他接口
- App Router 页面层继续负责读取真实授权列表并处理安全降级，客户端叶子组件只负责 `create/revoke` 写操作与本地回显，不把整页降成客户端取数
- TDD 顺序保持明确：先让 API 集成测试失败在“缺 revoke 路由/缺状态回显”，再让 Web 测试失败在“缺真实列表/缺创建撤销交互”，最后补最小实现并跑通 `api/web build`

这样能同时保住房间授权的审计历史、聊天室页的真实状态展示，以及 Next.js 服务端读/客户端写的稳定边界。

### 25. 老师工作台客户端裁定后，先保留首屏真相，再本地派生刷新

当老师工作台页面采用“服务端读队列 + 客户端按钮裁定”的组合时，不要只在客户端覆写单条 item 的文案；否则 `待裁定 / 已裁定 / 重点关注` 统计会和列表状态分叉。

- 首屏渲染时保留服务端返回的 `summary` 作为老师进入页面时看到的真相，不要在 hydration 阶段立刻自行重算把后端统计改掉
- 客户端组件应把 `summary` 与 `items` 收拢成同一份本地队列状态；一旦 `POST /reviews/decide` 成功，就只更新目标项，再从本地队列重新派生三项统计
- 如果后端列表项里暂时没有显式“是否待老师裁定”字段，可在前端用 `summary.pendingCount` 对前几条可裁定项做最小推导，但要把这种推导视为过渡方案，而不是长期业务真相
- TDD 先钉住两个行为：点击裁定后单条 item 的 `decision/finalScore` 更新，以及 summary 中 `pendingCount/reviewedToday/flaggedCount` 同步变化
- 风险提示要写清楚：当前端必须推导待裁定态时，后续应优先推动后端补齐显式字段，避免“已通过但仍待裁定”的语义混淆长期留在 Web 层

这样能让老师在不重新请求后端的前提下获得即时反馈，同时把“首屏真相”和“本地交互回显”分成两个清晰阶段，减少列表项已变更但 summary 仍停留旧值的错位。

### 26. 登录态先收口成“后端真相 + 浏览器快照”，页面层负责读取，叶子组件不承载会话协议

当教学平台第一次把真实登录态贯穿到 Web 和 API 时，不要急着把 token、角色判断和 session 校验塞进 `chat-room`、`review-queue` 这类叶子组件；应先把会话真相收口在后端 `current session` 接口与浏览器 session 快照里，再让页面层决定当前用户是谁、该请求哪份数据。

- 后端先提供最小 `GET /auth/session` 能力，用 `Bearer token` 读取 `UserSession` 和当前用户，保证“当前是谁”有后端真相来源
- 浏览器端本地存储先只保存最小 session 快照，例如 `token + user`，它服务于页面恢复与跳转，不替代后端校验
- 页面层优先负责读取当前用户并映射到真实查询参数，例如聊天页根据当前学生 session 决定 `studentId` 和 `roomId`
- 首页、聊天页、老师工作台这类入口页可以共享一个轻量 session banner 或同等页面级能力，但不要把登录协议扩散进提交按钮、裁定按钮等叶子组件
- 如果某个叶子组件内部把首屏 props 落进本地 state，页面层应优先通过 remount key 或 props 边界解决异步首屏刷新，而不是顺手把会话逻辑一起塞进去

这样能把“登录态成立”“页面知道当前是谁”“叶子组件继续保持薄交互层”三件事分开处理，既让最小真实登录链路尽快落地，也避免后续聊天室、老师工作台和更多入口页各自长出一套分叉的会话协议。

### 27. E2E 优先跑生产态 Web 服务，不把稳定性押在 `next dev`

当 Playwright 烟雾测试已经主要承担“入口可打开、降级文案可见、核心页面不崩”这类稳定性验收时，不要继续让它默认依赖 `next dev`；开发服务器上的热更新、客户端 manifest 与内部调试能力更适合联调，不适合作为仓库级验收基线。

- E2E 配置优先改成“先 `next build`，再 `next start --port <e2e-port>`”，例如用 `pnpm e2e:server` 统一收口，而不是直接把 `webServer.command` 指向 `next dev`
- 端口纪律保持不变：开发 Web、E2E Web、API 继续使用彼此独立的固定端口，避免切到生产态 server 后又把端口边界打乱
- 先用一个最小配置测试钉住 `playwright.config.ts` 的 `baseURL`、`webServer.url` 与启动命令，防止后续有人为图省事改回 `dev:e2e`
- 若仓库曾出现 React Client Manifest、`__webpack_modules__[moduleId] is not a function`、或只在浏览器测试链路出现的 Next 内部异常，先怀疑 `next dev` 在测试场景下引入了不必要的不稳定层，而不是立刻回退页面功能
- 最终验收仍以仓库根命令为准：修复后至少同时跑通 `pnpm test`、`pnpm build`、`pnpm test:e2e`，证明“配置测试转绿”和“真实生产态烟雾测试转绿”两层都成立

这样能把本地联调体验与仓库级稳定性验收清楚分层，减少因为 `next dev` 的开发期行为把 E2E 冒烟结果变成偶发红灯。

### 28. 本地 session 只是浏览器快照，服务端页面守卫还需要可读 token 通道

当 Next.js App Router 页面需要在服务端根据“当前是谁”决定是否继续请求老师工作台、个人聊天室这类真实数据时，不要把登录态只存在 `localStorage`；服务端读不到它，页面就会被迫回退成客户端兜底、默认学生 ID 或匿名空态。

- 浏览器端仍可保留 `localStorage` 里的完整 session 快照，用于客户端 banner、恢复跳转与最小交互状态
- 但同时要额外同步一份最小 token 到服务端可读介质，例如 `cookie`，让 Server Component 能先读取 session，再决定是否继续请求受保护接口
- 页面守卫优先发生在服务端：未登录直接显示登录提示，角色不符直接显示权限提示，而不是先请求老师/学生数据再在客户端回滚
- API client 应支持“同一条读接口既可匿名取公共数据，也可带 `Authorization` 读受保护数据”，避免页面层为了加 header 复制一套 fetch 逻辑
- 测试要同时覆盖两层真相：登录后确实写入可被服务端读取的 token，以及老师页/聊天页在无 token、学生 token、老师 token 三种情况下分别呈现正确入口状态

这样能把“浏览器里看起来已登录”和“服务端页面真的知道当前用户是谁”统一起来，避免聊天室继续静默回退到 `student-1`，也避免老师工作台在匿名状态下误拉真实审核数据。

### 29. 授权列表不只是可读可写，还必须认当前房主；待裁定状态不只是统计数，还必须显式下发到项级

当系统已经有了登录态、老师工作台和聊天室授权列表后，不要把“有接口”误当作“真规则已经成立”。如果 `/rooms/access-grants` 任何登录学生都能读写别人的房间授权，或 `/reviews` 只有 `summary.pendingCount` 没有项级待裁定标记，前端最终还是会靠猜测拼业务真相。

- 房间授权接口要基于当前 session 识别房主，再决定是否允许 `list/create/revoke`；仅有 `roomId` 参数不足以代表调用者真的有权操作该房间
- 即使当前阶段房间归属还是通过稳定命名约定推导，也要先把“只有房主能管授权历史”这条规则钉进后端，而不是继续让前端默认谁都能改
- 老师工作台读模型应为每条审核项显式返回 `isPendingTeacherDecision` 或等价字段，避免 Web 继续用 `summary.pendingCount` 反推前几条“看起来像待裁定”的项
- 页面层可以继续做展示映射，例如把 `isPendingTeacherDecision=true` 映射成“待老师裁定”，但待裁定真相必须以后端布尔字段为准
- TDD 顺序保持明确：先让非房主访问授权历史返回 403，再让老师队列组件测试失败在“缺显式 pending 字段”，最后补最小实现并跑通全仓 `test/build/e2e`

这样能把“授权型进入聊天室”和“老师工作台以后端真相为准”两条核心产品规则真正落到后端边界，而不是停留在页面文案或前端推导层。

### 30. 像素办公室场景升级先抓人物真实性、镜头尺度与伪 3D 层次

当像素办公室场景已经能表达“这里是办公区”，但仍让人觉得像功能示意图而不是一个真实空间时，不要优先继续堆图标、文案或更多家具；应先同时检查三件事：人物是否像真实人在空间里活动、镜头尺度是否给了场景足够的沉浸感、以及地面与家具是否形成了可感知的纵深层次。

- 人物真实性优先看“姿态差异 + 空间关系”，不要所有角色都用同一套站姿或坐姿；至少要区分坐着办公、站着交谈、路过移动、在休息区停留这几类状态
- 角色摆放不要只按功能点均匀分布，而要按真实办公室的动线布置：工位成组、过道留白、会客区有人经过、中心区域要有主角位
- 如果用户觉得“画面太小”，优先怀疑镜头覆盖范围过大或单个对象体积过小；应先放大主体区、减少无效地面占比、让首屏直接看到核心场景，而不是先继续往边缘加内容
- 像素伪 3D 不一定靠真 3D 引擎，先用俯视角里的“墙带 + 地面分材质 + 家具前后遮挡 + 阴影/底座 + 高低层级”做出空间纵深，再决定是否进一步上更复杂素材
- 顶部墙面、主工位群、中心工位、休息区这类块面必须有明确主次；布局先建立“大块关系”，再补画框、书架、饮水机、文件柜这类细节道具
- 当参考图明显更成熟时，优先借鉴它的“构图模板”和“资产语义”，例如双人工位岛、墙面展示带、休息区材质切换，而不是逐像素抄某个小道具
- 优化顺序建议固定为：先调镜头与布局块面，再调人物比例与姿态，再补伪 3D 层次，最后才补素材细节与氛围装饰

这样能把“已经有像素办公室”进一步提升成“看起来像真实办公室世界”，避免在错误层级上反复打磨却始终缺少沉浸感。

### 30. 提交鉴权先锁“当前学生真相”，再校验 body 回显与会话归属

当 `POST /submissions` 这类学生写接口开始接真实登录态时，不要只要求前端带一个 `studentId` 就直接入库；也不要只验证“已登录”却放过借用别人的 `agentSessionId`。提交命令的真实操作者必须以后端当前 session 为准，body 里的学生标识只作为需要被校验的回显字段。

- 提交接口优先要求学生登录，匿名请求直接 401，老师或其他非学生角色即使已登录也不能代学生提交
- `body.studentId` 必须等于当前 session 对应学生，用来防止前端篡改 payload 冒充别的学生
- `agentSessionId` 必须先查归属，再确认它属于当前学生，而不是只依赖外键存在或让数据库报错兜底
- 绿灯路径仍应返回原有提交响应契约，但落库时以当前 session 学生为准，不把 `body.studentId` 当业务真相来源
- TDD 顺序保持明确：先让集成测试失败在“匿名可提交 / studentId 可冒用 / agentSessionId 可越权复用”，再补最小控制器鉴权与归属校验

这样能把“谁有权提交”“提交代表谁”“这次提交绑定哪个 Agent 会话”三层真相同时收拢到后端边界，避免教学平台出现学生互相代投、借 session 提交或前端 payload 看起来合法但身份真相已漂移的问题。

### 31. 评审状态先分成 queued、ai-reviewed、teacher-decided，再让页面只做映射

当教学平台开始把“提交即固定 85 分通过”的占位逻辑替换成真实评审链路时，不要继续把 AI 初评和老师终裁揉在同一时刻写进数据库；也不要只给前端一个 `pendingCount`，让它自己猜哪些项还在 AI 评审、哪些项已到老师手里。

- 学生提交时先创建 `queued` review 快照，响应里明确返回“评审已入队”，而不是直接假装已经 `approve`
- AI 处理阶段单独把 review 从 `queued` 推进到 `ai-reviewed`，在这一刻才补上 `suggestedScore`、建议 `decision` 和 AI `rationale`
- 老师裁定阶段再把 review 推进到 `teacher-decided`，此时才写入 `reviewerId`、最终 `decision`、`decidedAt` 与最终分数
- 队列处理器必须只消费 `queued` 项，不能把已经被老师裁定的记录再次写回 `ai-reviewed`，否则会出现异步任务回滚业务真相的竞态
- Web 页面只消费显式 `reviewStatus` 并做文案映射，例如 `queued -> AI评审中`、`ai-reviewed -> 待老师裁定`、`teacher-decided -> 已通过/需调整/已退回`；不要继续靠 `summary` 或空值组合推导状态

这样能把“已受理”“AI 已初评”“老师已终裁”三层业务真相彻底拆开，让老师工作台、学生端轮询和后续实时通知都围绕同一份后端状态机扩展，而不是继续依赖同步占位分数或前端猜测。

### 32. 聊天室先锁房间成员边界，再补最小消息流

当教学平台准备把聊天室从“只读概览”推进到“可进入、可留言”的真实协作空间时，不要先做完整 IM；先把“谁能进这个房间”钉成后端规则，再补最小消息持久化与页面视角切换。

- 房间读取与写消息都应统一复用同一条成员判定：只有房主本人或拥有 `approved` 且未过期授权的学生，才能读取历史消息或发送新消息
- `GET /chat` 这类读模型一旦开始支持 `roomId`，就要在响应里显式返回 `viewerRole`、`roomId` 与 `messages[]`，让 Web 不再靠“当前登录人是否等于房主”自行猜视角
- 消息模型在早期阶段优先保持最小：先落 `roomId`、`authorId`、`body`、`createdAt`，等真正出现附件、系统消息、已读状态需求时再扩
- Server Component 页面继续负责按 `roomId` 取真实房间数据并做安全降级；`ChatRoom` 这类客户端叶子组件只负责发送消息与本地回显，不接管首屏取数
- TDD 顺序保持明确：先让“未授权进他人房间 403”“已授权可读历史消息”“房间成员可发文本消息”三类测试失败，再补 Prisma 模型、访问校验和最小 UI

这样能先把聊天室建立在可解释的房间成员真相上，再逐步扩展实时广播、消息类型和更细的权限模型，避免一开始就做出“谁都能看、谁都能发”的假协作空间。

---

## 33. 开发执行协议

本节是所有编码 Agent 在本仓库执行开发任务时的操作协议，不覆盖用户明确给出的任务范围。

### 33.1 开始任务

1. 先读根目录 `AGENTS.md`、`CLAUDE.md`、`README.md`、目标 workspace 的 `package.json`，再搜索目标模块和测试。
2. 先执行 `git status --short`，理解用户已有修改；禁止覆盖、回滚或清理未由当前任务产生的变更。
3. 把任务拆成契约、后端、前端/世界、Connector、测试、文档六类，并标出依赖顺序。
4. 跨多个业务域时先写短计划；先确认业务真相、权限边界和失败状态，再写 UI 或动画。

### 33.2 Agent 战队分工

| Agent | 负责范围 | 必须交付 |
|---|---|---|
| 产品/教学 | 用户路径、Day 任务、验收标准 | 需求拆解、验收清单 |
| 架构 | 模块边界、共享契约、事件和数据流 | 接口草案、依赖图、风险 |
| 后端 | NestJS、Prisma、队列、WebSocket、鉴权 | API、数据变更、服务测试 |
| 前端 | Next.js、React、API client、页面状态 | 组件、页面、Web 测试 |
| 世界/视觉 | Phaser、布局、角色、家具、动效、HUD | 场景代码、视觉验收说明 |
| Connector | 本机 Agent 适配器、白名单命令、事件上报 | 适配器、边界测试、README |
| 测试/发布 | 定向测试、构建、E2E、回归报告 | 命令、输出、失败分类 |

Agent 之间只能通过任务、文件边界、共享契约和 handoff 协作。handoff 至少说明：完成内容、修改文件、验证命令、未解决问题、下一步依赖。

### 33.3 并发规则

- 可以并发：只读探索、不同 workspace 的测试分析、文档整理、互不重叠的前后端设计。
- 不要并发：同一文件、Prisma schema、共享契约、测试 fixture 的编辑。
- 共享契约和数据库 schema 先确定，再并行实现消费者。
- 默认最多两个 Agent 同时写代码；由一个集成 Agent 解决冲突并运行任务级验证。

### 33.4 实现规则

- 新行为先写失败测试或契约测试；测试验证业务行为，不只验证实现细节。
- API 输入先经过共享 Zod schema 或 DTO；响应字段与 `packages/contracts` 一致。
- Controller 保持薄层，业务规则放 Service；页面组合数据，展示组件不自行拼业务真相。
- Server Component 负责服务端读，客户端叶子组件负责按钮写操作。
- Phaser 只做世界表现；评分、权限、提交、任务解锁和教师裁定不得只存在 Phaser 内存中。
- 复杂场景先确认坐标系、深度顺序、缩放和窄屏行为，再补视觉细节。
- 手工修改文件必须使用 `apply_patch`；新文件默认 ASCII，产品文案除外。

### 33.5 Connector 安全

- API 不执行学生机器上的任意 shell；本机命令只能由 Connector 执行。
- Connector 只允许明确命令白名单、工作区根目录和可审计参数；优先使用 `spawn(command, args)`，禁止拼接未经校验的 shell 字符串。
- 不读取、不提交、不上报 API key、token、cookie、SSH 私钥或其他密钥。
- 配对令牌、心跳、事件和任务结果必须带用户/Agent scope，禁止使用全局默认学生。
- 删除、推送、发布、改库等高风险动作必须显式审批或使用独立受限适配器。

### 33.6 验证顺序

1. `git diff --check`
2. `pnpm --filter contracts test && pnpm --filter contracts build`
3. 直接指定文件的定向 Vitest/Playwright 测试
4. `pnpm --filter api build`、`pnpm --filter web build`、必要时 `pnpm --filter agent-connector build`
5. `pnpm test`、`pnpm build`、`pnpm test:e2e`

失败时记录命令、首个真实错误、是否由本次改动引入、已尝试修复和剩余风险。历史基线失败不能被悄悄改成通过。

### 33.7 交付格式

- **方案**：一句话说明业务结果和技术方式。
- **修改**：按模块列出路径和目的。
- **验证**：只列实际执行的命令及其结果。
- **风险**：只列真实存在且影响验收的问题。
- **下一步**：最多三项；没有自然下一步时不添加。

没有实际运行的命令不得写成“通过”，没有实际实现的能力必须标为“规划中”或“部分实现”。
