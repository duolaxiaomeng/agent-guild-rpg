{
  "name": "agent-connector",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "bin": {
    "agent-guild-connector": "dist/cli.js"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "vitest run",
    "start": "node dist/cli.js"
  },
  "devDependencies": {
    "@types/node": "^24.0.15",
    "vitest": "^3.2.4"
  }
}
